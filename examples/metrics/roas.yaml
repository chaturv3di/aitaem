metric:
  name: roas
  description: Return on ad spend — revenue generated per dollar of ad spend
  source: duckdb://ad_campaigns.duckdb/ad_campaigns
  aggregation: ratio
  numerator: "SUM(revenue)"
  denominator: "SUM(ad_spend)"
